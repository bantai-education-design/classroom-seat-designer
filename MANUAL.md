# 学級座席デザイナー v1.0.0 使い方ガイド

「学級座席デザイナー」は、小学校の担任の先生が、日々の授業や学級経営に合わせて児童名簿から座席表を簡単に作成・調整し、印刷できるアプリケーションです。

> 🆓 本アプリは**無料公開版**です。ライセンスキーの入力やアカウント登録は一切不要で、そのままお使いいただけます。

### 🚀 Web版 公開URL
**[https://bantai-education-design.github.io/classroom-seat-designer/](https://bantai-education-design.github.io/classroom-seat-designer/)**

### 💻 Windowsポータブル版（インストール不要）
Windowsでオフライン利用したい場合は、GitHub Releases で配布しているポータブル版（`学級座席デザイナー-v1.0.0-portable.exe`）をご利用ください。ZIPを解凍し、exeファイルをダブルクリックするだけで起動します。

> [!NOTE]
> 未署名アプリのため、初回起動時にWindows Defender SmartScreenの警告画面が表示される場合があります。「詳細情報」→「実行」を選ぶと起動できます（詳しくは本ガイド末尾の「初回起動時のWindowsセキュリティ警告について」を参照してください）。

---

## 📅 おおまかな使い方の流れ

座席表を作る基本的な流れは以下の通りです。

1. **名簿を準備する**
2. **児童の情報を入力する**（直接入力、またはCSVファイルを読み込みます）
3. **学年・学級名を入力する**
4. **座席形態（1人席・2人組・班席）を選ぶ**
5. **自動で配置する**（またはドラッグ＆ドロップで直接並び替えます）
6. **必要に応じて「固定席」や「配慮条件」を設定する**
7. **用途（教師用・掲示用）に合わせて印刷（PDF保存）する**
8. **JSON保存で座席配置データを保存する**

---

## 📝 主な機能と使い方の説明

### 1. 名簿編集とCSV読み込み
画面左上の「名簿編集」ボタンから、クラスの児童名簿を登録できます。

* **直接編集**: 出席番号、氏名、ふりがな、性別、身長をその場で編集できます。
* **CSVファイルの読み込み**: Excelなどで作成したCSV名簿を一括で取り込むことができます。
  * **低学年の先生へ**: ふりがなを入力しておくと、後述の「ひらがな表示」機能でひらがなの座席表が作成できます。姓と名の間に半角または全角スペースを入れておくのがおすすめです。

#### 💡 取り込み用CSVファイルの形式
以下のような形式のCSVファイル（カンマ区切り）に対応しています。

```csv
出席番号,氏名,ふりがな,性別,身長
1,佐藤 大翔,さとう ひろと,男,128
2,鈴木 陽葵,すずき ひまり,女,122
3,高橋 陽翔,たかはし はると,男,131
```

---

### 2. 座席形態の選択
右側の設定パネルから、学級活動に合わせて3つの座席形態を選択できます。

* **1人席**: 通常の授業や個別の学習、テストを行うときに適しています。
* **2人組席**: ペア学習や隣同士の席替え、隣の児童との短い話し合い活動（ペアワーク）を頻繁に行う際に向いています。ペアごとに点線の枠やペア番号が表示されます。
* **班席**: グループ学習、給食の班、掃除や生活班活動の際に便利です。2×2の4人グループ（班）ごとに太い外枠と班番号が表示されます。

---

### 3. 自動配置と調整
座席を自動で並び替える便利な補助機能です。

* **ランダムに配置**: 完全なクジ引きのように配置します。
* **身長順（低い子を前方へ）**: 前方希望の児童や身長の低い児童を自然に前方・中央付近に集め、後ろの児童が見づらくならないように配置します。
* **男女をなるべく交互に**: 男女が極力交互になるようにバランスよく配置します。

> [!NOTE]
> 自動配置は、担任の先生の「配置決め作業」を助けるための**補助機能（たたき台作り）**です。自動配置を実行した後に、児童のセルをドラッグ＆ドロップして、最終的な位置の調整を行ってください。

---

### 4. 固定席の設定
視力や健康上の都合、個別の事情などで「この席から動かしたくない」という児童がいる場合に使用します。
* 画面上で対象児童の席にある「固定」ボタンを押すことで、その席にロックされます。
* 固定された児童は、自動配置を実行してもその場所から動きません。

---

### 5. 配慮条件と配慮確認（教師用画面のみ）
自動配置のアルゴリズムや、先生の手動調整時に役立つ条件を設定できます。
* **前方希望**: 自動配置の際に、対象児童を最優先で前方の席に配置します。
* **近づけたい子**: 隣同士や近くの席に配置する条件です（席が離れている場合、画面上に「確認」メッセージが表示されます）。
* **離したい子**: 近くの席に配置しない条件です（席が近づいてしまった場合、画面上に「注意」メッセージが表示されます）。

> [!IMPORTANT]
> これらの配慮条件は、担任の先生のための**内部極秘情報**です。子供たちに見せる「掲示用きれい版」では完全に非表示になり、印刷にも出ませんのでご安心ください。

---

### 6. 名前表示とサイズの設定
子供たちの学年や用途に応じて、見え方を細かくカスタマイズできます。

* **名前表示**:
  * **漢字**: 通常の表示です。
  * **ひらがな**: 低学年（1・2年生）や特別支援学級などで、子供たち自身が自分の席をすぐに確認できるよう、名簿の「ふりがな」を使ってひらがなで表示します。
* **名前サイズ**:
  * **標準**: 通常の読みやすいサイズです。
  * **大きめ**: 文字を少し大きくし、視認性を高めます。
  * **低学年向け**: ひらがな表示などの際に、よりはっきりと見えやすくなるように文字サイズを極限まで拡大します（長い名前でも自動的にセル内に収まるよう微調整されます）。

---

### 7. 印刷形式の使い分け
「教師用」と「掲示用」の2種類のレイアウトを切り替えて印刷（PDF保存）できます。

* **教師用チェック記入型**:
  * 出席番号、性別、身長のほか、児童ごとの「チェック欄（日々の提出物確認や小テストの記録用）」が表示されます。
  * 画面上部に、先生向けの「配慮確認（注意・確認メッセージ）」が一緒に印刷されます。
* **掲示用きれい版**:
  * 児童名（および班番号・ペア番号）のみが大きく表示されます。余計なチェック欄や性別、配慮情報などは一切印刷されません。
  * 教室の後ろの黒板や掲示板に貼って、子供たちに席を知らせるのに最適です。

---

## 💾 データの「保存」と「読み込み」について

本アプリは、個人情報の保護と安全のため、データを外部のサーバーに保存しません。代わりに、先生のパソコン内にファイルを保存する仕組みをとっています。2つの保存形式を使い分けてください。

* **CSVファイル（名簿用）**:
  * 出席番号や児童氏名、身長などの「児童名簿リスト」そのものを保存・編集するためのファイルです。Excelで開いて編集することができます。
* **JSONファイル（座席・設定全体用）**:
  * 児童名簿だけでなく、「誰がどの席に座っているか」「固定席の位置」「設定した配慮条件」「学年・学級名などの設定」を含む**すべての状態をそのまま丸ごと保存**するためのファイルです。席替えの途中で作業を一時保存したい場合や、過去の座席表を記録しておきたい場合は、必ずこの「JSON保存」を行ってください。次回「JSON読込」をすることで完全に作業を再開できます。

---

## ⚠️ ご利用上の注意事項

1. **印刷前の確認**:
   「配慮条件」などの極秘情報は掲示用には印刷されない設計ですが、万が一の誤操作を防ぐため、**印刷ボタンを押した後の印刷プレビュー画面で、余計な情報が出ていないかを必ず先生の目で確認**してから印刷・配布を行ってください。
2. **個人情報の安全管理**:
   名簿データや座席データ（CSV・JSON）は先生のパソコンのローカルディスクに保存されます。USBメモリの紛失などによる児童の個人情報漏洩を防ぐため、作成したファイルの取扱いや管理には十分にご注意ください。
3. **無料公開版について**:
   本アプリは無料公開版です。ライセンス認証は不要で、価格は発生しません。Web版・Windowsポータブル版のいずれも同じ機能を無料でご利用いただけます。

## 💻 Windowsデスクトップアプリ（exe）版について

本アプリは、インターネットに接続できない教室やオフライン環境でも利用できるように、Windows用のポータブル実行ファイル（exeファイル）として動作するデスクトップ版も用意されています。

### 📌 Web版との違いと特徴
* **オフライン利用**: インターネット接続が不要で、ローカル環境で直接起動できます。
* **データ保存**: Web版と同様、児童名簿や座席データはすべてローカル上のCSV/JSONファイルで保存・読込を行います。外部サーバーには一切データを送信しません。
* **印刷機能**: 印刷時やPDF保存時の挙動、教師用/掲示用のレイアウト切り替え、印刷プレビューなど、Web版と同じ機能がすべてローカルで動作します。

### ⚠️ 初回起動時のWindowsセキュリティ警告について
未署名アプリ（野良アプリ）としてパッケージされているため、初回起動時にWindowsの保護機能（Microsoft Defender SmartScreen）によって警告画面が表示される場合があります。

1. 青い画面で **「Windows によって PC が保護されました」** と表示されます。
2. 画面上の **「詳細情報」** というテキストリンクをクリックします。
3. アプリケーション名が「学級座席デザイナー」であることを確認し、右下に現れる **「実行」** ボタンを押すことで、次回以降は警告なしで起動できるようになります。

> [!IMPORTANT]
> 印刷レイアウトの崩れや、CSV/JSONの読み書きエラーがないか、事前によくテストしてから授業等の本番でご利用ください。

---
